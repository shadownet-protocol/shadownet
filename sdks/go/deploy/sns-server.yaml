# Sample sns-server config for the Docker Compose dev stack.
did: "did:web:sns.shadownet.example"
provider: "shadownet.example"
listen: "0.0.0.0:8443"
signing:
  keyfile: "/etc/shadownet/provider.jwk"
storage:
  driver: sqlite
  dsn: "/var/lib/shadownet/sns.db"
defaultTtl: 300
