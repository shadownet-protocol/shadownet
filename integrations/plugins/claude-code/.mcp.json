{
  "mcpServers": {
    "shadownet": {
      "command": "${CLAUDE_PLUGIN_ROOT}/bin/mcp-shadownet-proxy.py",
      "env": {
        "SHADOWNET_CONNECT_URL": "${user_config.connect_url}"
      }
    }
  }
}
