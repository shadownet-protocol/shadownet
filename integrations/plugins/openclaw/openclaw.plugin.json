{
  "id": "shadownet",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "required": ["endpoint", "token"],
    "properties": {
      "endpoint": {
        "type": "string",
        "format": "uri",
        "description": "Per-tenant MCP endpoint (e.g. https://<your-sidecar>/u/<your-shadowname>/mcp). RFC-0008-compliant Sidecars serve a pre-filled snippet at https://<your-sidecar>/connect/openclaw."
      },
      "token": {
        "type": "string",
        "minLength": 16,
        "writeOnly": true,
        "description": "Bearer token for the MCP endpoint. Mint on your sidecar's connect page."
      }
    }
  }
}
