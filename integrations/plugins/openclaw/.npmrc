# Don't auto-install peerDependencies. The only peer is `openclaw`, which
# OpenClaw itself provides at runtime — we never want it on a developer's
# machine (it pulls 530MB+ of transitive deps). Compile-time types come
# from local stubs in `types/openclaw.d.ts`.
auto-install-peers=false
